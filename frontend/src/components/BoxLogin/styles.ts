import styled from "@emotion/styled";

export const BackgroundArea = styled.div(() => ({
  height: "100%",
  background: "linear-gradient(0deg, rgba(111,21,255,1) 0%, rgba(22,119,255,1) 100%)",
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
}));

export const LoginBox = styled.div(() => ({
  width: 380
}));
